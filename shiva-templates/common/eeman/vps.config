<?xml version='1.0' standalone='yes'?>
<subnets>
<list subnets as subnet>
  <subnet addr="${subnet.addr}"
<if subnet.device>
          device="${subnet.device}"
</if>                    
          gateway="${subnet.gateway}"
          mask="${subnet.mask}"/>
</list>
</subnets>
